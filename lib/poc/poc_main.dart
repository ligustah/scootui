import 'package:flutter/material.dart';

import 'poc_screen.dart';

/// The entry point for the map renderer Proof of Concept.
///
/// To run this, use the command:
/// flutter run -t lib/poc/poc_main.dart
void main() {
  runApp(const PocScreen());
}
